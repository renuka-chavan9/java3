apiVersion: v1
kind: Service
metadata:
  name: medical-svc

spec:
  type: NodePort

  selector:
    app: Renuka-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
