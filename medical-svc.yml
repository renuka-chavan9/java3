apiVersion: v1
kind: Service
metadata:
  name: medical-svc

spec:
  selector:
    app: Renuka-app

  type: NodePort

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30644
